#!/bin/bash

REF=${1-origin/$(get_main_branch)}
TOPLEVEL=$(git rev-parse --show-toplevel)
for file in $(git diff --name-only $REF | sed "s@^@$TOPLEVEL/@"); do
  realpath --relative-to=$(pwd) $file
done
