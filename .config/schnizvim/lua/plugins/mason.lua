-- Mason: LSP/DAP/Linter/Formatter package manager
-- Loaded on-demand via commands, not at startup

return {
  "williamboman/mason.nvim",
  cmd = { "Mason", "MasonInstall", "MasonUpdate", "MasonUninstall", "MasonLog" },
  opts = {},
}
